module.exports = function (userConfig) {
    var newConfig = {
        paths: {
            src: 'src',
            dist: 'dist',
            tmp: '.tmp',
            e2e: 'e2e'
        }
    }

    return newConfig;
}